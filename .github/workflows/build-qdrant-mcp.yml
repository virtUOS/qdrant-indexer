name: Auto Update qdrant-mcp

on:
  schedule:
    - cron: '0 6 * * *'  # daily at 6am UTC
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/qdrant-mcp

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:

      - name: Checkout
        uses: actions/checkout@v6

      - name: Normalize image name
        id: meta
        run: echo "image_name=${IMAGE_NAME,,}" >> $GITHUB_OUTPUT

      - name: Get latest mcp-server-qdrant version from PyPI
        id: pypi
        run: |
          VERSION=$(curl -sf https://pypi.org/pypi/mcp-server-qdrant/json | jq -r '.info.version')
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Latest mcp-server-qdrant version on PyPI: ${VERSION}"

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if image tag already exists
        id: check
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ steps.meta.outputs.image_name }}:${{ steps.pypi.outputs.version }}
        run: |
          if docker manifest inspect "${IMAGE}" > /dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Image ${IMAGE} already exists — skipping build."
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Image ${IMAGE} not found — proceeding with build."
          fi

      - name: Set up Docker Buildx
        if: steps.check.outputs.exists == 'false' || github.event_name == 'workflow_dispatch'
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        if: steps.check.outputs.exists == 'false' || github.event_name == 'workflow_dispatch'
        uses: docker/build-push-action@v7
        with:
          context: ./qdrant-mcp
          push: true
          build-args: |
            MCP_SERVER_QDRANT_VERSION=${{ steps.pypi.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ steps.meta.outputs.image_name }}:${{ steps.pypi.outputs.version }}
            ${{ env.REGISTRY }}/${{ steps.meta.outputs.image_name }}:latest
